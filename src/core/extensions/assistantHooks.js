export function onDraftGenerated(_draft, _context = {}) {
  // Extension hook placeholder for future assistant integrations.
}

export function onExportRequested(_format, _payload = {}) {
  // Extension hook placeholder for future assistant integrations.
}

export function onProfileUpdated(_profile, _context = {}) {
  // Extension hook placeholder for future assistant integrations.
}
